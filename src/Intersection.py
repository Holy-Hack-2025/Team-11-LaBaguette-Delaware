class Intersection:
    def __init__(self, routes = []):
        """Initiates the intersection with the chosen layout"""
        self.routes = routes
        
    def add_route(self, id, length, trafic_light_position, cars_on_start):
        
    
